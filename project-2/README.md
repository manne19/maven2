# Welcome to Tango-Controls server project-2


Clone this repo and build single executable jar using maven:

```bash
$> mvn package
```

Run the server:

```bash
$> java -jar target/project-2-1.0-SNAPSHOT.jar

```